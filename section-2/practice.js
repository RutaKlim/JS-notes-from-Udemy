/*
// TO ONLY ACCEPT IF ITS A NUMBER OR STRING
const arrayName = [3, -2, -6, -1, 'error', 9, 13, 17, 'tommy', 14, 9, 5];
const validArray = [];
for (let i = 0; i < arrayName.length; i++) {
   if (typeof arrayName[i] !== 'number') continue;
  // if (typeof arrayName[i] === 'string') continue;
  validArray.push(arrayName[i]);
}
console.log(validArray);
*/
